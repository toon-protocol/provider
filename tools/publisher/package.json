{
  "name": "toon-provider-directory-publisher",
  "version": "0.1.0",
  "private": true,
  "description": "Pays for a TOON provider's relay writes, so the provider app never holds a payment channel",
  "license": "Apache-2.0",
  "type": "module",
  "main": "publish.mjs",
  "scripts": {
    "start": "node publish.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@toon-protocol/client": "^3.3.0"
  }
}
